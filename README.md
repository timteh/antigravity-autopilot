# antigravity-autopilot
🚀 Auto-accept agent steps in Antigravity IDE — no CDP required. Uses OS-level accessibility (Windows UI Automation) to click Run/Accept/Continue buttons invisibly. Works when every other auto-accept extension fails.
